{
    "name": "andrey-helldar/bad-browser-detect",
    "description": "Determine the minimum acceptable version of the browser and notify the user if the version does not match the required version.",
    "type": "library",
    "license": "MIT",
    "keywords": ["bad browser"],
    "authors": [
        {
            "name": "Andrey Helldar",
            "email": "helldar@ai-rus.com"
        }
    ],
    "require": {
        "php": ">=5.6.9",
        "illuminate/database": "^5.4|^6.0|^7.0",
        "illuminate/bus": "^5.4|^6.0|^7.0",
        "illuminate/http": "^5.4|^6.0|^7.0",
        "illuminate/queue": "^5.4|^6.0|^7.0",
        "illuminate/contracts": "^5.4|^6.0|^7.0",
        "illuminate/mail": "^5.4|^6.0|^7.0",
        "illuminate/notifications": "^5.4|^6.0|^7.0",
        "jaybizzle/crawler-detect": "^1.2",
        "jenssegers/agent": "^2.6",
        "nesbot/carbon": "^1.22|^2.0"
    },
    "suggest": {
        "symfony/thanks": "Give thanks (in the form of a GitHub) to your fellow PHP package maintainers"
    },
    "autoload": {
        "psr-4": {
            "Helldar\\BadBrowser\\": "src"
        },
        "files": [
            "src/helpers/helpers.php"
        ]
    },
    "config": {
        "preferred-install": "dist"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "extra": {
        "branch-alias": {
            "dev-master": "1.0-dev"
        },
        "laravel": {
            "providers": [
                "Helldar\\BadBrowser\\ServiceProvider"
            ]
        }
    }
}
